import { Box, Button, IconButton, Typography } from '@mui/material';
import { useState } from 'react';

interface ButtonProps {
  title: string;
  iconSrc: string;
  type: string;
}

export const CustomButton = ({ title, iconSrc, type }: ButtonProps) => {
  return (
    // 너비, 기능, 색상에 따른 다양한 타입의 버튼 확장
    // type에 따라 다른 스타일 적용
  )
}
